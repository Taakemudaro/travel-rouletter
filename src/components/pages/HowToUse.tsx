import { FC, memo } from "react";
import { Footer } from "../organisms/Footer";

/**
 * 利用方法ページを表示するコンポネント
 *
 * @returns 利用方法ページUIのJSX要素
 */
export const HowToUse: FC = memo(() => {
  return (
    <>
      <p>利用方法ページだよ〜〜〜</p>
    </>
  );
});
